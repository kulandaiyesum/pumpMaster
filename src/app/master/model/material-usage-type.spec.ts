import { MaterialUsageType } from './material-usage-type';

describe('MaterialUsageType', () => {
  it('should create an instance', () => {
    expect(new MaterialUsageType()).toBeTruthy();
  });
});
